org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=true

# Platform — versions mirror a known-good Create 6.x / NeoForge 1.21.1 build.
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.21.2)
neo_version=21.1.233
neo_version_range=[21.1.181,21.2)
loader_version_range=[4,)

# Mod identity
mod_id=simulacra
mod_name=Create: Simulacra
mod_version=1.0.0
mod_license=MIT
mod_authors=akuro
mod_description=A Create add-on where surplus rotational power becomes compute — a mechanical "neural network" datacenter that trains models of mobs and simulates them for loot, with zero spawned entities.

# Create + its dependency stack
create_version=6.0.10-281
create_version_range=[6.0.0,6.1.0)
registrate_version=MC1.21-1.3.0+67
ponder_version=1.0.82
ponder_version_range=[1.0.0,2.0)
flywheel_version=1.0.6
flywheel_version_range=[1.0.0,2.0)
vanillin_version=1.1.3-41

# Dev-only: JEI for recipe inspection in the test client (not compiled against, not shipped)
jei_version=19.27.0.343
